package com.example.degreeplanner.ui.home;

import androidx.lifecycle.ViewModel;

import com.example.degreeplanner.classes.Schedule;

public class SharedHomeViewModel extends ViewModel {

    // Create a schedule to hold quarters
    Schedule mySchedule = new Schedule();

    // Create a certain amount of quarters based on welcome screen input and add to schedule

    // Method to add course from plan popup to quarter

}